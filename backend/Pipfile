[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Core FastAPI dependencies
fastapi = "==0.104.1"
uvicorn = {extras = ["standard"], version = "==0.24.0"}
pydantic = "==2.5.0"
pydantic-settings = "==2.1.0"

# Neo4j driver with vector support
neo4j = "==5.17.0"

# Neo4j GraphRAG for advanced retrieval
neo4j-graphrag = "==1.10.0"

# HTTP requests for Solr integration
requests = "==2.31.0"
httpx = "==0.25.2"

# Additional utilities
python-dotenv = "==1.0.0"
jinja2 = "==3.1.2"

# Vector embeddings and ML
sentence-transformers = "==2.7.0"
numpy = "==1.26.0"
torch = ">=2.0.0"
transformers = ">=4.35.0"
scikit-learn = "==1.3.2"

# Async support
asyncio = "==3.4.3"
aiofiles = "==23.2.1"

# Logging and monitoring
structlog = "==23.2.0"
rich = "==13.7.0"

# Optional: Performance monitoring
psutil = "==5.9.7"

[dev-packages]
# Development and testing
pytest = "==7.4.0"
pytest-asyncio = "==0.21.0"
pytest-cov = "==4.1.0"
black = "==23.12.1"
isort = "==5.13.2"
mypy = "==1.7.1"

# Development tools
pre-commit = "==3.6.0"
ruff = "==0.1.6"

[requires]
python_version = "3.8"

[scripts]
start = "uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
test = "pytest tests/ -v --cov=backend --cov-report=term-missing"
lint = "black . && isort ."
type-check = "mypy backend/ --ignore-missing-imports"
format = "black . && isort ."